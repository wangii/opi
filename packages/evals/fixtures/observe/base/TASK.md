# Observe fixture

This repository snapshot contains a deliberately small shared-state module.
Inspect the implementation and test before changing it. Treat the test as the
observable contract and keep hypotheses separate from verified behavior.
